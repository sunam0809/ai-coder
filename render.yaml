services:
  - type: web
    name: ai-coder-api
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter @workspace/api-server run build
    startCommand: node artifacts/api-server/dist/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: SESSION_SECRET
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: DATABASE_URL
        sync: false

  - type: web
    name: ai-coder-frontend
    runtime: static
    region: oregon
    plan: free
    buildCommand: npm install -g pnpm && pnpm install --frozen-lockfile && BASE_PATH=/ PORT=3000 pnpm --filter @workspace/ai-coder run build
    staticPublishPath: artifacts/ai-coder/dist/public
    routes:
      - type: rewrite
        source: /api/*
        destination: https://ai-coder-api.onrender.com/api/$1
      - type: rewrite
        source: /*
        destination: /index.html
